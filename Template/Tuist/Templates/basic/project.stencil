import ProjectDescription
import ProjectDescriptionHelpers

let name = "{{ name }}"

let project = Project.feature(
    name: name,
    packages: [],
    targets: [
        Target.makeAdditionalTarget(name: name, bundleId: "com.\(name).\(name)")
    ]
)
